// This matrix maps each invoice (by its position in the invoices array)
// to the corresponding client (by position in the clients array).
// invoice at index 0 belongs to client at index 0, etc.
// Built against the ORIGINAL unsorted array order.

export const invoiceClientMatrix: number[] = [0, 1, 2]
